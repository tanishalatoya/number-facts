import React, { useSate } from 'react';

const SearchForm = () => {
    const [queryValue, updatequeryValue] = useSate('')
    
    return(
        <form>

        </form>
    )
}

export default SearchForm;